{
  "name": "nodejs-cicd-demo",
  "version": "1.0.0",
  "scripts": {
    "test": "jest",
    "build:dev": "rm -rf dev && cp -R public dev",
    "build:prod": "rm -rf docs && cp -R public docs",
    "deploy:dev": "gh-pages -d dev -b gh-pages",
    "deploy:prod": "gh-pages -d docs -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "jest": "^29.5.0"
  }
}
